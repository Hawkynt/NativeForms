<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <AssemblyName>Hawkynt.NativeForms.Tests</AssemblyName>
    <RootNamespace>Hawkynt.NativeForms.Tests</RootNamespace>
    <IsPackable>false</IsPackable>
    <IsTestProject>true</IsTestProject>
    <!-- GtkNativeSizingTests writes GdkEvent fields through a pointer to inject real input. -->
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.NET.Test.Sdk" Version="17.11.1" />
    <PackageReference Include="NUnit" Version="4.2.2" />
    <PackageReference Include="NUnit3TestAdapter" Version="4.6.0" />
  </ItemGroup>

  <ItemGroup>
    <Using Include="NUnit.Framework" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\NativeForms.Core\NativeForms.Core.csproj" />
    <!-- For GtkNativeSizingTests, which asserts against real GTK widgets; the fixture skips itself
         on any machine without a display, so the reference costs nothing elsewhere. -->
    <ProjectReference Include="..\NativeForms.Backends.Gtk\NativeForms.Backends.Gtk.csproj" />
    <!-- Pure [LibraryImport] declarations, so it builds and loads anywhere; the fixture that uses it
         self-skips off Windows, which is what keeps the suite green on every runner. -->
    <ProjectReference Include="..\NativeForms.Backends.Windows\NativeForms.Backends.Windows.csproj" />
    <!-- The property-grid populator source generator (compile-time only; no runtime assembly). -->
    <ProjectReference Include="..\NativeForms.Generators\NativeForms.Generators.csproj"
                      OutputItemType="Analyzer" ReferenceOutputAssembly="false" />
  </ItemGroup>

</Project>
