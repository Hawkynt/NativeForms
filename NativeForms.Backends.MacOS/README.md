# Hawkynt.NativeForms.Backends.MacOS

[![NuGet](https://img.shields.io/nuget/v/Hawkynt.NativeForms.Backends.MacOS)](https://www.nuget.org/packages/Hawkynt.NativeForms.Backends.MacOS/)
[![NuGet downloads](https://img.shields.io/nuget/dt/Hawkynt.NativeForms.Backends.MacOS)](https://www.nuget.org/packages/Hawkynt.NativeForms.Backends.MacOS/)
[![License](https://img.shields.io/badge/license-LGPL--3.0--or--later-blue)](https://www.gnu.org/licenses/lgpl-3.0.html)
[![CI](https://img.shields.io/github/actions/workflow/status/Hawkynt/NativeForms/ci.yml?branch=main&label=CI)](https://github.com/Hawkynt/NativeForms/actions/workflows/ci.yml)

> The macOS backend for NativeForms. Published so the backend set is complete and referenceable -- it is not implemented yet, and every entry point throws.

## 📦 Installation

```bash
dotnet add package Hawkynt.NativeForms.Backends.MacOS
```

This is a backend for [NativeForms](https://www.nuget.org/packages/Hawkynt.NativeForms/). Install the core package too — it carries the controls.

## ✨ Features

- Renders [NativeForms](https://www.nuget.org/packages/Hawkynt.NativeForms/) controls on macOS through Cocoa.
- None yet. The type surface exists so consuming code can compile and register against it.
- Reflection-free and source-generated, so it survives trimming and NativeAOT.
- Referenced but never registered, the whole backend is dropped again by the trimmer.

## 🚀 Quick start

Register the backend before creating a window. Registering more than one is fine: an application can
carry every platform it ships on.

```csharp
using Hawkynt.NativeForms;
using Hawkynt.NativeForms.Backends;
using Hawkynt.NativeForms.Backends.MacOS;

BackendRegistry.Register(new CocoaBackend());

var form = new Form { Text = "Hello", Bounds = new(0, 0, 320, 160) };
Application.Run(form);
```

## 📚 API reference

<!-- API:BEGIN generated by Hawkynt/RepositoryTemplate/package-readme — edit the XML docs in source, not here -->

### Namespace `Hawkynt.NativeForms.Backends.MacOS`

[`CocoaBackend`](#cocoabackend)

#### `CocoaBackend`

The macOS (Cocoa/AppKit) backend. This is currently a wired-but-unimplemented placeholder: it reports support on macOS and fails with an explicit, actionable message rather than pretending to draw. Implementing it — `NSApplication`, `NSWindow`, `NSButton`, `NSTextField` via `objc_msgSend` P/Invoke — is tracked in `docs/PRD.md`.

Implements `IPlatformBackend`.

| Member | Signature | Summary |
| --- | --- | --- |
| `CocoaBackend` | `CocoaBackend()` |  |
| `ButtonRendersImageWithText` | `bool ButtonRendersImageWithText { get; }` |  |
| `IsSupported` | `bool IsSupported { get; }` |  |
| `Name` | `string Name { get; }` |  |
| `Theme` | `ITheme Theme { get; }` |  |
| `CreateButton` | `IButtonPeer CreateButton()` |  |
| `CreateCanvas` | `ICanvasPeer CreateCanvas()` |  |
| `CreateCheckBox` | `ICheckBoxPeer CreateCheckBox()` |  |
| `CreateComboBox` | `IComboBoxPeer CreateComboBox()` |  |
| `CreateGroupBox` | `IGroupBoxPeer CreateGroupBox()` |  |
| `CreateImage` | `IImage CreateImage(int width, int height, ReadOnlySpan<int> argb)` |  |
| `CreateLabel` | `ILabelPeer CreateLabel()` |  |
| `CreateLinkLabel` | `ILinkLabelPeer CreateLinkLabel()` |  |
| `CreateListBox` | `IListBoxPeer CreateListBox()` |  |
| `CreateNotifyIcon` | `INotifyIconPeer CreateNotifyIcon()` |  |
| `CreatePopup` | `IPopupPeer CreatePopup(IWindowPeer owner)` |  |
| `CreateProgressBar` | `IProgressBarPeer CreateProgressBar()` |  |
| `CreateRadioButton` | `IRadioButtonPeer CreateRadioButton()` |  |
| `CreateRichTextBox` | `IRichTextBoxPeer CreateRichTextBox()` |  |
| `CreateScrollBar` | `IScrollBarPeer CreateScrollBar(bool vertical)` |  |
| `CreateTextBox` | `ITextBoxPeer CreateTextBox()` |  |
| `CreateTimer` | `ITimerPeer CreateTimer()` |  |
| `CreateTrackBar` | `ITrackBarPeer CreateTrackBar(bool vertical)` |  |
| `CreateWindow` | `IWindowPeer CreateWindow()` |  |
| `GetClipboardText` | `string GetClipboardText()` |  |
| `GetDpiScale` | `double GetDpiScale()` |  |
| `GetScreenSize` | `Size GetScreenSize()` |  |
| `MeasureText` | `Size MeasureText(string text, Font font)` |  |
| `Post` | `void Post(Action action)` |  |
| `PumpEvents` | `int PumpEvents(int limit = 64)` | Runs whatever is waiting in the application's queue through one turn of this loop — the toolkit's interception first, then AppKit's own dispatch — and answers how many events it handled. |
| `Quit` | `void Quit()` |  |
| `Run` | `void Run(IWindowPeer mainWindow)` |  |
| `SetClipboardText` | `void SetClipboardText(string text)` |  |
| `ShowColorDialog` | `Color? ShowColorDialog(Color color)` |  |
| `ShowFileDialog` | `string[] ShowFileDialog(in FileDialogOptions options)` |  |
| `ShowFontDialog` | `Font? ShowFontDialog(Font font)` |  |
| `ShowMessageBox` | `DialogResult ShowMessageBox(string text, string caption, MessageBoxButtons buttons, MessageBoxIcon icon, IWindowPeer owner = null)` |  |
| `ThemeChanged` | `event EventHandler ThemeChanged` |  |

<!-- API:END -->

## 🔌 Dependencies

| Package | Why |
| --- | --- |
| [`Hawkynt.NativeForms`](https://www.nuget.org/packages/Hawkynt.NativeForms/) | The controls, layout and backend abstraction this package implements. |

**Runtime requirement:** macOS, once implemented.

## ⚠️ Limitations

- **Not implemented.** Every entry point throws. Do not ship an application that depends on this backend to render.
- It is published for completeness and to keep the backend set referenceable; progress is tracked in docs/PRD.md.

## ❤️ Support

If this project saves you time or money, consider supporting its development:

[![GitHub Sponsors](https://img.shields.io/badge/GitHub-Sponsor-EA4AAA?logo=githubsponsors)](https://github.com/sponsors/Hawkynt)
[![PayPal](https://img.shields.io/badge/PayPal-Donate-00457C?logo=paypal)](https://www.paypal.me/hawkynt)

## 📜 License

Licensed under LGPL-3.0-or-later — see the repository [LICENSE](https://github.com/Hawkynt/NativeForms/blob/main/LICENSE).
